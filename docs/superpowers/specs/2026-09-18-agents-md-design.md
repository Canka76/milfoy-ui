# Specification: Root AGENTS.md Onboarding Guide

**Date:** 2026-09-18  
**Status:** Approved  
**Topic:** Root `AGENTS.md` to instruct visiting AI agent swarms on Milfoy's architecture, headless CLI export, spatial occlusion analysis, and unit testing protocols.

---

## 1. Overview & Objectives
Provide visiting AI coding agents with direct, actionable instructions on how to interact with the Milfoy UI Depth Inspector repository, run headless CLI diagnostic exports, understand zero-GC C# patterns, and execute the NUnit test suite.

---

## 2. AGENTS.md Content Structure
- **Core Identity**: Milfoy uGUI 3D Exploded Viewport & Diagnostic Engine.
- **Headless AI CLI**: Copy-pasteable Unity batchmode commands for extracting token-optimized JSON/Markdown context (`-dumpMode anomalies`).
- **Architecture Notes**: `UIDiagnosticAnalyzer` (spatial occlusion pre-computation), `UIAIContextExporter` (`fixAction` JSON schemas), and `UIElementEntry`.
- **Testing Protocol**: Running NUnit tests via Unity Editor test mode.
